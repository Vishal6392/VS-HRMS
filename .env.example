PORT=5000
NODE_ENV=development
# MONGODB_URI is optional locally. If empty, the app runs an automatic in-memory MongoDB!
# For production (Render / Atlas), set: mongodb+srv://<user>:<password>@cluster.mongodb.net/vs-hrms
MONGODB_URI=
JWT_SECRET=vs_hrms_jwt_secret_production_key_2026_987654321
CLIENT_URL=http://localhost:5173
